services:
  - type: web
    name: macvaarai
    env: docker
    region: oregon
    plan: free
    rootDir: macvaarai-backend
    dockerfilePath: macvaarai-backend/Dockerfile
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
